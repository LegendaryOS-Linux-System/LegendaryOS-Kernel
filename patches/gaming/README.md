# patches/gaming/

Patche gamingowe stosowane na kod źródłowy kernela (`patch -p1`).

Przykład: ręczny patch BORE scheduler gdy `auto_fetch_patches = false` w config.toml.
Pobierz z: https://github.com/firelzrd/bore-scheduler
